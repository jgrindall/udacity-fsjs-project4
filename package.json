{
    "name": "udacity",
    "version": "0.0.0",
    "scripts": {
        "client": "npm run client:install & npm run client:build & npm run client:deploy",
		"client:install": "cd client & npm i",
		"client:build": "cd client & npm run build",
		"client:deploy": "cd client && npm run deploy"
    }
}
