{
    "name": "udacity",
    "version": "0.0.0",
    "scripts": {
		
        "client:install": "cd client && npm i",
		"client:build": "cd client && npm run build",
		"client:deploy_aws": "cd client && npm run deploy_aws",
		

		"server:deploy_aws": "cd server && npm run deploy_aws"
    }
}
