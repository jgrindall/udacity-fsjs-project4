{
    "name": "udacity",
    "version": "0.0.0",
    "scripts": {
		"client_install": "cd client & npm i",
		"client_build": cd client & npm run build",
		"client_deploy": "cd client && npm run deploy"
    }
}
